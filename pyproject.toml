[project]
name = "planilla"
version = "0.1.0"
description = "Calculo de planilla mensual. Proyecto base de la Hoja de Trabajo 3."
requires-python = ">=3.13"
dependencies = []

[project.scripts]
planilla = "planilla.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/planilla"]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-cov>=6.0",
    "ruff>=0.8",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
# 80%: cubre con margen todas las reglas de negocio y la CLI sin exigir el
# 100% en la guarda trivial `if __name__ == "__main__"` de cli.py.
addopts = "--cov=planilla --cov-report=term-missing --cov-report=html --cov-fail-under=80"

[tool.coverage.run]
source = ["src/planilla"]

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F"]
